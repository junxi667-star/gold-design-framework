window.JEWELCHAIN_CONFIG = Object.freeze({
  // 本地运行时留空，API 与网页同源。
  // Cloudflare Pages 部署包中会改成 https://api.jewelchain.xyz
  apiBaseUrl: "",
  deploymentMode: "local-or-same-origin",
  siteName: "JewelChain Studio"
});
