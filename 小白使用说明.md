# JewelChain Studio v1.0.0 小白使用说明

## 一、第一次怎么启动

### 1. 解压

把压缩包完整解压到一个纯英文或简单中文路径，例如：

```text
D:\JewelChain\jewelchain-studio-v1.0.0-hackathon-final
```

不要直接在 ZIP 压缩包里双击运行。

### 2. 启动全部服务

双击：

```text
START_JEWELCHAIN.bat
```

它会自动完成：

```text
启动 Master API
→ 启动 Image Worker
→ 打开浏览器
```

浏览器地址通常是：

```text
http://127.0.0.1:4173/
```

### 3. 判断是否启动成功

页面顶部应显示：

```text
Master 与生图端已就绪
生图执行端：Image Worker 在线（1）
```

如果只显示“Master 已启动，Worker 未上线”，等待 5 秒后刷新。

## 二、怎么生成 V1

在需求框输入：

```text
设计一款适合年轻女性日常佩戴的新中式黄金戒指，使用简化祥云元素，不要太复杂。
```

点击：

```text
让 Agent 生成 V1
```

后台流程：

```text
Master 创建任务
→ WebSocket 推送给 Worker
→ Worker 调用 Seedream
→ Worker 上传图片
→ Master 保存 V1
```

页面会显示进度。不要连续重复点击。

## 三、怎么登记 V1 到 Monad

1. 点击“连接钱包”；
2. 选择 MetaMask；
3. 按提示切换到 Monad Testnet；
4. 点击“登记 V1 到 Monad”；
5. 在 MetaMask 中确认交易；
6. 等待页面显示“已登记 Monad”。

钱包必须有少量测试 MON。

## 四、怎么生成 V2

V1 上链成功后，在修改要求中输入：

```text
保留戒圈和祥云元素，把表面改成磨砂质感。
```

点击：

```text
基于最新链上版本生成 V2
```

V2 会保存：

```text
parentContentHash = V1 contentHash
```

这表示 V2 是从 V1 修改而来。

## 五、怎么完成最终版本

1. 点击“登记 V2 到 Monad”；
2. MetaMask 确认；
3. 等待登记成功；
4. 点击“设为最终确认版”；
5. 再次在 MetaMask 中确认。

完成后可以：

- 查看 V1/V2 时间线；
- 打开 Monad Explorer；
- 查看 Metadata；
- 下载最终凭证 JSON；
- 询问 Agent 最终版本和父子关系。

## 六、怎么停止

双击：

```text
STOP_JEWELCHAIN.bat
```

它会同时停止：

```text
Image Worker
Master API
```

## 七、出现问题怎么办

### 页面打不开

运行：

```text
DIAGNOSE_PROJECT.bat
```

### Worker 不在线

先双击：

```text
STOP_JEWELCHAIN.bat
```

再双击：

```text
START_JEWELCHAIN.bat
```

查看日志：

```text
logs\image-worker.log
```

也可以运行：

```text
RUN_IMAGE_WORKER.bat
```

这样错误会直接显示在窗口里。

### 生图失败

运行：

```text
TEST_ARK_API.bat
```

当前包保留了原有 API 配置。不要把 `.env` 发到群里，也不要上传公开 GitHub。

### 钱包交易失败

检查：

- MetaMask 是否连接 Monad Testnet；
- 是否有测试 MON；
- `.env` 中合约地址是否正确；
- Monad RPC 是否可访问。

## 八、以后上云怎么改

现在不用做。上云时只需拆成两边：

```text
云服务器：运行 Master
你的电脑：运行 Image Worker
```

电脑 `.env` 修改：

```env
MASTER_BASE_URL=https://api.jewelchain.xyz
```

云端和本地的 `WORKER_TOKEN` 必须一致。

API Key 建议只留在本地 Worker，不放在云服务器，也不放前端。
